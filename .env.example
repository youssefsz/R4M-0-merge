# GitHub API Authentication Token
# Get a token from https://github.com/settings/tokens
# Optional but recommended to avoid low unauthenticated rate limits (60 req/hour)
GITHUB_TOKEN=your_github_token_here
